# ideas/ — deferred product notes

This directory holds product ideas that are worth keeping but are not active work.

Use Solo todos for work that is planned, scheduled, blocked, or needs coordination.
Use `ideas/` for concepts that should survive cleanup of the todo backlog but do not
currently justify an implementation task.

## Notes

- [`agent-playground.md`](./agent-playground.md) — deferred idea for an authenticated
  agent/chat playground with inspectable Raven `search`/`execute` calls.
